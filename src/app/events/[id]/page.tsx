/**
 * Equal to ./[slug]/page - simply incorporates EventInfo, which fetches the event, and redirects to its correct slug
 * if required.
 */
import EventInfo from './components/EventInfo';
export default EventInfo;
