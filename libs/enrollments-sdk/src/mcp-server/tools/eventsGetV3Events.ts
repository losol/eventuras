/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsGetV3Events } from "../../funcs/eventsGetV3Events.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV3EventsRequest$inboundSchema,
};

export const tool$eventsGetV3Events: ToolDefinition<typeof args> = {
  name: "events_get-v3-events",
  description: `Retrieves a list of events based on the given query.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsGetV3Events(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
