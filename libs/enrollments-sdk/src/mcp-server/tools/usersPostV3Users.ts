/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersPostV3Users } from "../../funcs/usersPostV3Users.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV3UsersRequest$inboundSchema,
};

export const tool$usersPostV3Users: ToolDefinition<typeof args> = {
  name: "users_post-v3-users",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersPostV3Users(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
