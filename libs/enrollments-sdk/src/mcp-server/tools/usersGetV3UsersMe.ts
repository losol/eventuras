/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetV3UsersMe } from "../../funcs/usersGetV3UsersMe.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV3UsersMeRequest$inboundSchema,
};

export const tool$usersGetV3UsersMe: ToolDefinition<typeof args> = {
  name: "users_get-v3-users-me",
  description:
    `Gets information about the current user. Creates a new user if no user with the email exists.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersGetV3UsersMe(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
