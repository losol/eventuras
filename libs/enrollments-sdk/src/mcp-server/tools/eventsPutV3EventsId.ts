/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsPutV3EventsId } from "../../funcs/eventsPutV3EventsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV3EventsIdRequest$inboundSchema,
};

export const tool$eventsPutV3EventsId: ToolDefinition<typeof args> = {
  name: "events_put-v3-events-id",
  description: `Updates an existing event by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsPutV3EventsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
