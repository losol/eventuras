type: object
title: Notification recipients filter
description: |
  Used to filter out notification recipients.
  All properties are optional.
properties:
  eventId:
    type: integer
    format: int32
  registrationStatuses:
    type: array
    items:
      $ref: '../registrations/RegistrationStatus.yaml'
  registrationTypes:
    type: array
    items:
      $ref: '../registrations/RegistrationType.yaml'
