{
  "defaultAction": "SCMP_ACT_ERRNO",
  "comment": "Minimal seccomp profile for Chromium with user namespaces",
  "syscalls": [
    {
      "names": ["clone", "setns", "unshare"],
      "action": "SCMP_ACT_ALLOW",
      "comment": "Required for Chromium user namespace sandbox"
    }
  ]
}
