/**
 * Client-side configuration
 *
 * apps/web has NO NEXT_PUBLIC_* variables – all config is read server-side
 * at runtime and passed as props to client components where needed.
 *
 * This file is kept as a compatibility stub.
 */

// No client-side public env vars remain.
export const publicEnv = {} as Record<string, never>;
