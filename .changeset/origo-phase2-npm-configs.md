---
'@eventuras/web': patch
'@eventuras/conductor': patch
'@eventuras/event-sdk': patch
'@eventuras/google-api': patch
'@eventuras/mailer': patch
'@eventuras/markdown-plugin-happening': patch
'@eventuras/scribo': patch
'@eventuras/shipper': patch
'@eventuras/smartform': patch
---

Consume the shared foundation packages (`eslint-config`, `typescript-config`, `vite-config`, `logger`, `core`, `core-nextjs`, `app-config`) from npm — they are maintained in the origo repo now; the workspace copies are removed
